# План разработки: Админ-панель для системы маршрутных листов

## Оглавление

1. [Обзор проекта](#1-обзор-проекта)
2. [Архитектура](#2-архитектура)
3. [Стек технологий](#3-стек-технологий)
4. [Структура проекта](#4-структура-проекта)
5. [Система авторизации](#5-система-авторизации)
6. [Роли и права доступа](#6-роли-и-права-доступа)
7. [Страницы и функциональность](#7-страницы-и-функциональность)
8. [API-эндпоинты](#8-api-эндпоинты)
9. [Этапы разработки](#9-этапы-разработки)
10. [Запуск проекта](#10-запуск-проекта)
11. [Тестовые данные](#11-тестовые-данные)

---

## 1. Обзор проекта

Проект представляет собой веб-приложение (админ-панель) для управления маршрутными листами на производственном предприятии. Состоит из двух частей:

- **Backend** — ASP.NET Core Web API (.NET 8.0), обеспечивает REST API, JWT-авторизацию и работу с базой данных MS SQL Server.
- **Frontend** — React SPA (Vite + TypeScript + Tailwind CSS), предоставляет пользовательский интерфейс для работы с системой.

### Предметная область

Маршрутный лист — документ, описывающий последовательность технологических операций для изготовления изделия. Система автоматизирует:

- Планирование производства (позиции плана, изделия)
- Формирование и управление маршрутными листами
- Управление операциями (добавление, назначение исполнителей, смена статусов)
- Ведение справочников (цеха, типы операций, исполнители, единицы измерения)

---

## 2. Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    Браузер (клиент)                      │
│  ┌───────────────────────────────────────────────────┐  │
│  │         React SPA (Vite + TypeScript)             │  │
│  │  ┌──────────┐  ┌────────────┐  ┌──────────────┐  │  │
│  │  │  Страницы│  │ AuthContext │  │  API-клиент  │  │  │
│  │  │  (Pages) │  │ (JWT token)│  │   (axios)    │  │  │
│  │  └──────────┘  └────────────┘  └──────┬───────┘  │  │
│  └───────────────────────────────────────┼───────────┘  │
└──────────────────────────────────────────┼──────────────┘
                                           │ HTTP (JSON)
┌──────────────────────────────────────────┼──────────────┐
│                ASP.NET Core Web API                      │
│  ┌───────────┐  ┌────────────────┐  ┌────────────────┐  │
│  │   Auth    │  │  Controllers   │  │  JWT Middleware │  │
│  │ Controller│  │ (CRUD + бизнес)│  │ (проверка      │  │
│  │           │  │                │  │  токена)       │  │
│  └─────┬─────┘  └───────┬────────┘  └────────────────┘  │
│        │                │                                │
│  ┌─────┴────────────────┴───────────────────────────┐   │
│  │           Entity Framework Core 8.0              │   │
│  │              ApplicationDbContext                 │   │
│  └──────────────────────┬───────────────────────────┘   │
└─────────────────────────┼───────────────────────────────┘
                          │
                ┌─────────┴─────────┐
                │  MS SQL Server    │
                │  RoutingSheetsDB  │
                └───────────────────┘
```

### Взаимодействие компонентов

1. Пользователь открывает React SPA в браузере
2. При входе отправляется `POST /api/auth/login` с логином и паролем
3. Backend проверяет учётные данные, возвращает JWT-токен
4. Токен сохраняется в `localStorage` и подставляется в заголовок `Authorization: Bearer <token>` для каждого запроса
5. Backend валидирует токен через JWT middleware и обрабатывает запрос
6. При ответе 401 фронтенд автоматически перенаправляет на страницу логина

---

## 3. Стек технологий

### Backend

| Компонент               | Технология                                     |
|-------------------------|------------------------------------------------|
| Платформа               | .NET 8.0                                       |
| Тип приложения          | ASP.NET Core Web API                           |
| ORM                     | Entity Framework Core 8.0                      |
| База данных             | Microsoft SQL Server                           |
| Аутентификация          | JWT Bearer (Microsoft.AspNetCore.Authentication.JwtBearer 8.0) |
| Хеширование паролей     | BCrypt.Net-Next 4.1.0                          |
| Документация API        | Swagger / OpenAPI (Swashbuckle 6.4.0)          |
| Язык                    | C# 12                                          |

### Frontend

| Компонент               | Технология                    |
|-------------------------|-------------------------------|
| Сборщик                 | Vite 7.x                     |
| Фреймворк               | React 19 + TypeScript         |
| Стили                   | Tailwind CSS 4.x             |
| HTTP-клиент             | axios                        |
| Маршрутизация           | react-router-dom 7.x         |
| Управление запросами    | TanStack Query (React Query) |
| Язык                    | TypeScript                   |

---

## 4. Структура проекта

```
RoutingSheetsNew/                    # Корень репозитория
│
├── RoutingSheetsNew/                # Backend (.NET API)
│   ├── Controllers/
│   │   ├── AuthController.cs        # Аутентификация (login, me)
│   │   ├── RoutingSheetsController.cs
│   │   ├── OperationsController.cs
│   │   ├── PlanPositionsController.cs
│   │   ├── ProductItemsController.cs
│   │   ├── UnitsController.cs
│   │   ├── GuildsController.cs
│   │   ├── OperationTypesController.cs
│   │   ├── PerformersController.cs
│   │   ├── RoutingSheetStatusesController.cs
│   │   └── OperationStatusesController.cs
│   ├── Models/
│   │   ├── User.cs                  # Модель пользователя + роли
│   │   ├── RoutingSheet.cs
│   │   ├── Operation.cs
│   │   └── ...                      # Остальные модели
│   ├── DTOs/
│   │   ├── AuthDTOs.cs              # LoginDto, AuthResponseDto, UserDto
│   │   └── ...                      # Остальные DTO
│   ├── Data/
│   │   └── ApplicationDbContext.cs  # Контекст БД + seed-данные
│   ├── Migrations/
│   ├── Program.cs                   # Конфигурация: JWT, CORS, seed пользователя
│   ├── appsettings.json             # Настройки JWT + строка подключения к БД
│   └── RoutingSheetsNew.csproj
│
├── frontend/                        # Frontend (React SPA)
│   ├── src/
│   │   ├── api/
│   │   │   ├── client.ts            # Axios-инстанс с интерцепторами JWT
│   │   │   └── auth.ts              # API-функции авторизации
│   │   ├── components/
│   │   │   └── ProtectedRoute.tsx   # Защита маршрутов (редирект на логин)
│   │   ├── contexts/
│   │   │   └── AuthContext.tsx       # Контекст авторизации (user, login, logout)
│   │   ├── pages/
│   │   │   ├── LoginPage.tsx         # Страница входа
│   │   │   └── DashboardPage.tsx     # Дашборд (пока заглушка)
│   │   ├── types/
│   │   │   └── auth.ts              # TypeScript типы (User, LoginRequest, и т.д.)
│   │   ├── App.tsx                  # Маршрутизация приложения
│   │   ├── main.tsx                 # Точка входа
│   │   └── index.css                # Tailwind CSS
│   ├── index.html
│   ├── vite.config.ts               # Vite конфиг + proxy на API
│   ├── package.json
│   └── tsconfig.json
│
├── ПЛАН_РАЗРАБОТКИ.md               # Этот файл
└── RoutingSheetsNew.sln
```

---

## 5. Система авторизации

### Модель пользователя (User)

| Поле         | Тип     | Описание                         |
|--------------|---------|----------------------------------|
| Id           | int     | Первичный ключ                   |
| Username     | string  | Логин (уникальный, макс. 100)    |
| PasswordHash | string  | BCrypt-хэш пароля                |
| FullName     | string  | ФИО (макс. 300)                  |
| Role         | string  | Роль (макс. 50)                  |

### Процесс аутентификации

1. Клиент отправляет `POST /api/auth/login` с `{ username, password }`
2. Сервер ищет пользователя по `username`
3. Проверяет пароль через `BCrypt.Verify()`
4. Генерирует JWT-токен со следующими claims:
   - `NameIdentifier` — ID пользователя
   - `Name` — имя пользователя (username)
   - `GivenName` — ФИО
   - `Role` — роль
5. Возвращает `{ token, user: { id, username, fullName, role } }`

### Конфигурация JWT

| Параметр      | Значение                              |
|---------------|---------------------------------------|
| Issuer        | RoutingSheetsAPI                      |
| Audience      | RoutingSheetsClient                   |
| Алгоритм      | HMAC-SHA256                           |
| Время жизни   | 480 минут (8 часов — рабочий день)    |
| Хранение      | `localStorage` на клиенте             |

### Фронтенд: AuthContext

- Хранит текущего пользователя и токен
- При загрузке приложения проверяет токен вызовом `GET /api/auth/me`
- При ответе 401 автоматически очищает данные и перенаправляет на `/login`
- Предоставляет функции `login()` и `logout()` через React Context

---

## 6. Роли и права доступа

### Три роли в системе

| Код             | Название          | Описание                                       |
|-----------------|-------------------|-------------------------------------------------|
| WorkshopChief   | Начальник цеха    | Управление цехом, контроль маршрутных листов    |
| WorkshopForeman | Мастер цеха       | Операционное управление, назначение исполнителей |
| PlanningDept    | Плановый отдел    | Планирование, создание маршрутных листов        |

### Матрица прав (будет уточнена на следующих этапах)

| Действие                                | Начальник цеха | Мастер цеха | Плановый отдел |
|-----------------------------------------|:-:|:-:|:-:|
| Просмотр маршрутных листов              | + | + | + |
| Создание маршрутных листов              | ? | - | + |
| Редактирование маршрутных листов        | ? | - | + |
| Удаление маршрутных листов              | ? | - | + |
| Изменение статуса маршрутного листа     | + | - | + |
| Разбиение маршрутного листа             | ? | - | + |
| Просмотр операций                       | + | + | + |
| Создание/редактирование операций        | + | + | + |
| Назначение исполнителя                  | + | + | - |
| Изменение статуса операции              | + | + | - |
| Управление справочниками                | + | - | + |
| Управление позициями плана              | - | - | + |

> `+` — разрешено, `-` — запрещено, `?` — будет уточнено

---

## 7. Страницы и функциональность

### Этап 1 (реализован)

| Страница | Маршрут  | Описание                                          |
|----------|----------|---------------------------------------------------|
| Логин    | `/login` | Форма входа (логин + пароль), валидация, ошибки   |
| Дашборд  | `/`      | Приветствие, ФИО, роль, заглушки для будущих блоков |

### Этапы 2-N (план)

| Страница                    | Маршрут                     | Описание                                                   |
|-----------------------------|-----------------------------|-------------------------------------------------------------|
| Маршрутные листы (список)   | `/routing-sheets`           | Таблица с фильтрами, статусами, пагинацией                 |
| Маршрутный лист (детали)    | `/routing-sheets/:id`       | Просмотр/редактирование МЛ, список операций                |
| Создание маршрутного листа  | `/routing-sheets/new`       | Форма создания МЛ                                          |
| Операции по цеху            | `/operations`               | Список операций с фильтрами по цеху                        |
| Позиции плана               | `/plan-positions`           | CRUD таблица позиций плана                                 |
| Изделия                     | `/product-items`            | Список изделий (только просмотр)                           |
| Справочник: Цеха            | `/references/guilds`        | CRUD таблица цехов                                         |
| Справочник: Типы операций   | `/references/operation-types` | CRUD таблица типов операций                              |
| Справочник: Исполнители     | `/references/performers`    | CRUD таблица исполнителей                                  |
| Справочник: Ед. измерения   | `/references/units`         | CRUD таблица единиц измерения                              |
| Управление пользователями   | `/users`                    | CRUD пользователей (только администратор)                  |

---

## 8. API-эндпоинты

### Авторизация

| Метод | Путь              | Описание                           | Авторизация |
|-------|--------------------|-------------------------------------|-------------|
| POST  | `/api/auth/login`  | Вход в систему, получение JWT      | Нет         |
| GET   | `/api/auth/me`     | Получить текущего пользователя     | Да (JWT)    |

### Маршрутные листы

| Метод  | Путь                              | Описание                                  |
|--------|-----------------------------------|-------------------------------------------|
| GET    | `/api/RoutingSheets`              | Список МЛ (фильтр: `planPositionId`, `productItemId`) |
| GET    | `/api/RoutingSheets/{id}`         | МЛ по ID (с операциями)                   |
| POST   | `/api/RoutingSheets`              | Создать МЛ                                |
| PUT    | `/api/RoutingSheets/{id}`         | Обновить МЛ                               |
| DELETE | `/api/RoutingSheets/{id}`         | Удалить МЛ                                |
| PATCH  | `/api/RoutingSheets/{id}/status`  | Изменить статус                           |
| POST   | `/api/RoutingSheets/{id}/split`   | Разбить МЛ                                |

### Операции

| Метод  | Путь                                        | Описание               |
|--------|---------------------------------------------|------------------------|
| GET    | `/api/Operations`                           | Список (фильтр: `routingSheetId`, `guildId`) |
| GET    | `/api/Operations/by-routing-sheet/{id}`     | Операции по МЛ         |
| GET    | `/api/Operations/by-guild/{id}`             | Операции по цеху       |
| GET    | `/api/Operations/{id}`                      | Операция по ID         |
| POST   | `/api/Operations`                           | Создать                |
| PUT    | `/api/Operations/{id}`                      | Обновить               |
| DELETE | `/api/Operations/{id}`                      | Удалить                |
| PATCH  | `/api/Operations/{id}/assign-performer`     | Назначить исполнителя  |
| DELETE | `/api/Operations/{id}/performer`            | Снять исполнителя      |
| PATCH  | `/api/Operations/{id}/status`               | Изменить статус        |

### Справочники (полный CRUD)

| Ресурс             | Базовый путь             |
|---------------------|--------------------------|
| Единицы измерения  | `/api/Units`             |
| Цеха               | `/api/Guilds`            |
| Типы операций      | `/api/OperationTypes`    |
| Исполнители        | `/api/Performers`        |
| Позиции плана      | `/api/PlanPositions`     |

### Справочники (только чтение)

| Ресурс                    | Базовый путь                  |
|---------------------------|-------------------------------|
| Статусы маршрутных листов | `/api/RoutingSheetStatuses`   |
| Статусы операций          | `/api/OperationStatuses`      |
| Изделия                   | `/api/ProductItems`           |

### Статусы

**Маршрутные листы:** Черновик (1) → Активен (2) → Завершен (3) / Отменен (4)

**Операции:** Ожидает (1) → В работе (2) → Завершена (3) / Отменена (4)

---

## 9. Этапы разработки

### Этап 1: Авторизация + каркас фронтенда (ВЫПОЛНЕН)

**Backend:**
- [x] Модель `User` с ролями (`WorkshopChief`, `WorkshopForeman`, `PlanningDept`)
- [x] JWT-аутентификация (Bearer token)
- [x] `AuthController` (`POST /api/auth/login`, `GET /api/auth/me`)
- [x] BCrypt-хеширование паролей
- [x] CORS для фронтенда (`localhost:5173`)
- [x] Swagger с поддержкой JWT
- [x] Миграция для таблицы `Users`
- [x] Seed-данные: тестовый пользователь `chief / chief123`

**Frontend:**
- [x] Инициализация проекта (Vite + React + TypeScript + Tailwind CSS)
- [x] API-клиент (axios) с JWT-интерцепторами
- [x] `AuthContext` (хранение пользователя, login, logout, автопроверка токена)
- [x] Страница логина (форма, валидация, отображение ошибок)
- [x] Страница дашборда (заглушка с приветствием и информацией о пользователе)
- [x] `ProtectedRoute` (редирект неавторизованных на логин)
- [x] Маршрутизация (react-router-dom)
- [x] TanStack Query подключён (для будущих запросов)

### Этап 2: Маршрутные листы

- [ ] Страница списка маршрутных листов (таблица, фильтры по позиции плана / изделию)
- [ ] Страница деталей маршрутного листа (просмотр + редактирование)
- [ ] Создание нового маршрутного листа (форма с выбором позиции плана, изделия, ед. измерения)
- [ ] Смена статуса маршрутного листа
- [ ] Разбиение маршрутного листа (split)
- [ ] Удаление маршрутного листа с подтверждением

### Этап 3: Операции

- [ ] Список операций внутри маршрутного листа
- [ ] Создание/редактирование/удаление операций
- [ ] Назначение и снятие исполнителя
- [ ] Изменение статуса операции
- [ ] Просмотр операций по цеху (отдельная страница)

### Этап 4: Справочники

- [ ] CRUD для единиц измерения
- [ ] CRUD для цехов
- [ ] CRUD для типов операций
- [ ] CRUD для исполнителей
- [ ] CRUD для позиций плана производства
- [ ] Просмотр изделий (read-only)
- [ ] Просмотр статусов (read-only)

### Этап 5: Ролевая модель доступа

- [ ] Backend: `[Authorize(Roles = "...")]` на контроллерах
- [ ] Frontend: скрытие/блокировка элементов интерфейса в зависимости от роли
- [ ] Регистрация пользователей (только администратор)
- [ ] Страница управления пользователями

### Этап 6: Дашборд и UX

- [ ] Статистика на дашборде (количество МЛ по статусам, операции в работе и т.д.)
- [ ] Боковое меню навигации
- [ ] Уведомления (toast) при успешных/неуспешных операциях
- [ ] Пагинация и сортировка таблиц
- [ ] Адаптивная вёрстка

---

## 10. Запуск проекта

### Backend

```bash
cd RoutingSheetsNew/RoutingSheetsNew
dotnet run
```

API будет доступно по адресу: `https://localhost:7001`
Swagger UI: `https://localhost:7001/swagger`

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Фронтенд будет доступен по адресу: `http://localhost:5173`

> Vite проксирует все запросы `/api/*` на `https://localhost:7001`, поэтому для разработки достаточно запустить оба сервера.

### Порядок запуска

1. Убедитесь, что MS SQL Server запущен
2. Запустите бэкенд (`dotnet run`) — при первом запуске автоматически создастся БД, применятся миграции и добавится тестовый пользователь
3. Запустите фронтенд (`npm run dev`)
4. Откройте `http://localhost:5173` в браузере
5. Войдите с логином `chief` и паролем `chief123`

---

## 11. Тестовые данные

### Пользователи

| Логин   | Пароль    | ФИО                    | Роль            |
|---------|-----------|------------------------|-----------------|
| chief   | chief123  | Иванов Иван Петрович   | Начальник цеха  |

> Дополнительные пользователи с другими ролями будут добавлены на следующих этапах.

### Справочники (предзаполнены)

- **Единицы измерения:** шт., кг, м
- **Цеха:** Сборочный цех №1, Механический цех №2, Сварочный цех №3
- **Типы операций:** Сборка, Сварка, Токарная обработка, Фрезерная обработка, Контроль качества
- **Исполнители:** Иванов И.И. (Слесарь-сборщик), Петров П.П. (Сварщик), Сидоров С.С. (Токарь)
- **Изделия:** Корпус редуктора, Вал приводной, Шестерня ведущая
- **Позиции плана:** ПП-2024-001 (корпуса), ПП-2024-002 (валы), ПП-2024-003 (шестерни)
- **Маршрутные листы:** МЛ-2024-0001 (Активен, 4 операции), МЛ-2024-0002 (Черновик, 2 операции)

### Статусы маршрутных листов

| ID | Код        | Название   |
|----|------------|------------|
| 1  | DRAFT      | Черновик   |
| 2  | ACTIVE     | Активен    |
| 3  | COMPLETED  | Завершен   |
| 4  | CANCELLED  | Отменен    |

### Статусы операций

| ID | Код          | Название   |
|----|--------------|------------|
| 1  | PENDING      | Ожидает    |
| 2  | IN_PROGRESS  | В работе   |
| 3  | COMPLETED    | Завершена  |
| 4  | CANCELLED    | Отменена   |
